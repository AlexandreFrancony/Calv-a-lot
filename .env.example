# Calv-a-lot - Copy-Trading Follower pour Cash-a-lot
# Copier vers .env et remplir les valeurs

# === Leader (Cash-a-lot) ===
# URL du dashboard Cash-a-lot
# En Docker sur le même serveur : http://cashalot:8080
# En externe (via Pangolin) : https://crypto.francony.fr
LEADER_URL=http://cashalot:8080
# Secret partagé avec Cash-a-lot (demander à Alex)
SIGNAL_SECRET=

# === Binance API (tes propres clés, depuis binance.com) ===
BINANCE_API_KEY=
BINANCE_API_SECRET=
# Ne pas toucher — toujours false (le mode simulation gère le test sans risque)
BINANCE_TESTNET=false

# === Trading ===
# dry_run = simulation, live = vrai argent
TRADING_MODE=dry_run
# Budget initial en EUR
INITIAL_BUDGET_EUR=100
# Intervalle de polling en secondes (120 = 2 min)
POLL_INTERVAL_SECONDS=120

# === API Authentication (defense-in-depth) ===
# Générer le hash avec: python -c "from werkzeug.security import generate_password_hash; print(generate_password_hash('votre_mdp'))"
API_USER=admin
API_PASSWORD_HASH=

# === Email alerts (optionnel) ===
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
ALERT_EMAIL_TO=
